{
  "name": "ourspace-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --include=dev && npx tsc",
    "start": "cd backend && node dist/server.js"
  }
}
